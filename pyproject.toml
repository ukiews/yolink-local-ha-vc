[project]
name = "yolink-local-ha"
version = "0.2.0"
description = "Home Assistant integration for YoLink devices using the local API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1.0",
]
