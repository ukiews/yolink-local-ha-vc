[project]
name = "yolink-local-ha-vc"
version = "0.3.2"
description = "Home Assistant integration for YoLink devices using the local API"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "paho-mqtt>=2.1.0",
]
